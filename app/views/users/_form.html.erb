<%= form_for @user do |f| %>

	<%= f.label :email%>
	<%= f.text_field :email%>
	
	<%= f.label :encrypted_password, 'Password' %>
	<%= f.text_field :encrypted_password%>
	
	<%= f.submit %>
	
<%end%>
	
